apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: p4
spec:
  config: '{
      "cniVersion": "0.3.1",
      "name": "p4",
      "plugins": [
        {
          "type": "pod2pod"
        }
      ]
    }'
