apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: p1
spec:
  config: '{
      "cniVersion": "0.3.1",
      "name": "p1",
      "plugins": [
        {
          "type": "pod2pod"
        }
      ]
    }'
