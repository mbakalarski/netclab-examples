apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: p2
spec:
  config: '{
      "cniVersion": "0.3.1",
      "name": "p2",
      "plugins": [
        {
          "type": "pod2pod"
        }
      ]
    }'
